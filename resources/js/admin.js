// Admin main entry point
import './bootstrap'; // Assuming standard Laravel bootstrap

document.addEventListener('DOMContentLoaded', () => {
    console.log('Admin Dashboard loaded');
    
    // Toggle Sidebar on mobile if needed (example)
    // ...
});
